services:
  - type: web
    name: jeytech-api
    env: python
    plan: free
    region: frankfurt
    rootDir: backend
    buildCommand: pip3 install -r requirements.txt && alembic upgrade head
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
