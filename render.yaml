services:
  - type: web
    name: jeytech-api
    env: python
    plan: free
    region: frankfurt
    rootDir: backend
    buildCommand: bash build.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
