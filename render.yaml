services:
  - type: web
    name: jeytech-api
    env: python
    plan: free
    region: frankfurt
    rootDir: backend
    buildCommand: poetry install --no-root && alembic -c alembic.ini upgrade head
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
